node_modules/

# Environment variables
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# Internal docs (pricing, strategy) — README assets stay public
docs/*
!docs/logo.svg
!docs/example/

# OS
.DS_Store
Thumbs.db

# Output
*.jpg

# Reverse-engineering captures — contain live auth tokens, NEVER commit
*-api/captured-api.json
**/captured-api.json
whatsapp-api/
twitter-api/
*-api/auth.ts
# README demo capture: no auth data, safe to commit as an example
!docs/example/api/captured-api.json

# Secrets
secret
*.secret
.iframer/

# Lupe
.lupe/
.lupeignore
.lupeshared
.lupe-branch

# local API reverse-engineering scratch (not committed)
api-tests/
/benchmark/
/docs/todo/

# build output — published via npm \"files\", never committed (baked-path leak came from committing these)
/dist/
bin/*.cjs
